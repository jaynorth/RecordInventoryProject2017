<Window x:Class="RecordsWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RecordsWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="393.033" Width="679.918">
    <Grid>
        <DataGrid x:Name="dataGridx" HorizontalAlignment="Center" Margin="175,142,0,0" VerticalAlignment="Center"  ItemsSource="{ Binding  ListRecords}"
 Height="auto" Width="auto" AutoGenerateColumns="True"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="100,250,0,0" TextWrapping="Wrap" Text="{Binding CurrentArtist.Title}" VerticalAlignment="Top"/>
        <TextBlock x:Name="textBlock1" HorizontalAlignment="Left" Margin="175,266,0,0" TextWrapping="Wrap" Text="{Binding CurrentRecord.Title}" VerticalAlignment="Top" Width="270" Height="41"/>
    </Grid>
</Window>
